# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.1
loader_version=0.18.4
loom_version=1.15-SNAPSHOT
fabric_kotlin_version=1.13.9+kotlin.2.3.10

# Mod Properties
mod_version=1.0.2
maven_group=com.celestial_manta.betterlapras
archives_base_name=betterlapras

# Dependencies
fabric_api_version=0.116.9+1.21.1

# Cobblemon (Maven: https://maven.impactdev.net/repository/development/)
# Bump when you target a newer Cobblemon release for 1.21.1.
cobblemon_version=1.7.3+1.21.1

# Cobbreeding (Modrinth Maven). Optional at runtime; used for egg helper commands.
cobbreeding_version=2.2.1

# Cobbreeding runtime peers (not pulled transitively when using modRuntimeOnly; needed for runClient).
cloth_config_version=15.0.140