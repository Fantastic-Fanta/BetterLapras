package com.celestial_manta.betterlapras.content.blocks

/**
 * Register custom blocks and their block entities / behaviors here when added.
 */
object BlocksRegistration {
	fun register() {
		// Custom blocks with functions: register in BuiltInRegistries.BLOCK (and item) from this hook.
	}
}
